<views:ThemedWindow
    x:Class="GitX.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ae="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:GitX.Core.Models"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    xmlns:views="clr-namespace:GitX.Views"
    xmlns:vm="clr-namespace:GitX.ViewModels"
    Title="GitX - 分支对比"
    Width="1200"
    Height="800"
    MinWidth="800"
    MinHeight="500"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
    Background="{DynamicResource BgBrush}"
    FontFamily="{DynamicResource GlobalFont}"
    FontSize="13"
    Foreground="{DynamicResource TextBrush}"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="0"
            CornerRadius="6"
            GlassFrameThickness="0"
            NonClientFrameEdges="None"
            ResizeBorderThickness="6" />
    </shell:WindowChrome.WindowChrome>

    <DockPanel>
        <views:MainWindowHeader DockPanel.Dock="Top" />

        <!--  底部状态栏  -->
        <Border
            Padding="12,6"
            Background="{DynamicResource Bg2Brush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,1,0,0"
            DockPanel.Dock="Bottom">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="{DynamicResource TextDimBrush}"
                    Text="{Binding StatusBarText}" />
            </Grid>
        </Border>

        <!--  主体区域  -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="280"
                    MinWidth="160"
                    MaxWidth="500" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  左侧：差异目录树  -->
            <views:DiffTreePane x:Name="DiffTreePaneControl" Grid.Column="0" />

            <!--  分隔条  -->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />

            <!--  右侧：双栏 Diff 编辑器  -->
            <Grid Grid.Column="2" Background="{DynamicResource BgBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="5" />
                    <RowDefinition Height="280" MinHeight="220" />
                </Grid.RowDefinitions>

                <!--  加载状态  -->
                <Grid
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Panel.ZIndex="10"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock
                            Margin="0,0,0,12"
                            HorizontalAlignment="Center"
                            FontSize="48"
                            Text="⏳" />
                        <TextBlock
                            FontSize="14"
                            Foreground="{DynamicResource TextDimBrush}"
                            Text="正在计算差异..." />
                    </StackPanel>
                </Grid>

                <GridSplitter
                    Grid.Row="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{DynamicResource BorderBrush}"
                    ResizeBehavior="PreviousAndNext"
                    ShowsPreview="True" />

                <!--  文件头  -->
                <Border
                    Grid.Row="0"
                    Padding="12,8"
                    Background="{DynamicResource Bg2Brush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock
                                FontSize="13"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource AccentBrush}"
                                Text="{Binding SelectedNodeTitle}" />
                            <TextBlock
                                Margin="0,4,0,0"
                                FontSize="10"
                                Foreground="{DynamicResource TextDimBrush}"
                                Text="{Binding SelectedNodeSubtitle}" />
                        </StackPanel>

                        <StackPanel
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                            <Button
                                Margin="0,0,8,0"
                                Command="{Binding PreviousDiffCommand}"
                                Content="↑ 上一处"
                                IsEnabled="{Binding CanMoveToPreviousDiff}" />
                            <Button
                                Margin="0,0,8,0"
                                Command="{Binding NextDiffCommand}"
                                Content="↓ 下一处"
                                IsEnabled="{Binding CanMoveToNextDiff}" />
                            <Border
                                Margin="0,0,8,0"
                                Padding="8,2"
                                Background="{DynamicResource Surface2Brush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5">
                                <TextBlock
                                    FontSize="10"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AccentBrush}"
                                    Text="{Binding CurrentDiffPositionText}" />
                            </Border>
                            <Button
                                Command="{Binding OverwriteSelectedFileCommand}"
                                Content="⬇ 下载此文件"
                                Visibility="{Binding SelectedTreeNode.IsFile, Converter={StaticResource BoolToVisibility}}" />
                        </StackPanel>

                        <Border
                            Margin="0,6,0,0"
                            Padding="7,4"
                            Background="{DynamicResource BgBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="5"
                            Visibility="{Binding SelectedTreeNode, Converter={StaticResource NullToVisibility}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    FontSize="9"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource TextDimBrush}"
                                    Text="{Binding DiffOverviewText}"
                                    TextAlignment="Center" />
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!--  统一 diff 视图  -->
                <Grid Grid.Row="1">
                    <ae:TextEditor
                        x:Name="DiffEditor"
                        Background="{DynamicResource BgBrush}"
                        FontFamily="{DynamicResource CodeFont}"
                        FontSize="12"
                        Foreground="{DynamicResource TextBrush}"
                        HorizontalScrollBarVisibility="Auto"
                        IsReadOnly="True"
                        ShowLineNumbers="True"
                        VerticalScrollBarVisibility="Auto"
                        WordWrap="False" />

                    <!--  二进制文件提示  -->
                    <Grid Background="{DynamicResource BgBrush}" Visibility="{Binding IsBinaryFile, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock
                                Margin="0,0,0,12"
                                HorizontalAlignment="Center"
                                FontSize="48"
                                Text="📦" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                FontSize="14"
                                Foreground="{DynamicResource TextDimBrush}"
                                Text="二进制文件" />
                        </StackPanel>
                    </Grid>
                </Grid>

                <!--  提交记录底部面板  -->
                <Border
                    Grid.Row="3"
                    Margin="0,0,0,0"
                    Padding="12,10"
                    Background="{DynamicResource Bg2Brush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock
                                    FontSize="11"
                                    FontWeight="Bold"
                                    Foreground="{DynamicResource TextDimBrush}"
                                    Text="{Binding PathHistoryTitle}" />
                                <TextBlock
                                    Margin="0,3,0,0"
                                    FontSize="10"
                                    Foreground="{DynamicResource Accent2Brush}"
                                    Text="{Binding PathHistorySubtitle}" />
                            </StackPanel>
                            <TextBlock
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                FontSize="10"
                                Foreground="{DynamicResource TextDimBrush}"
                                Text="按时间筛选" />
                        </Grid>

                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                <TextBlock
                                    Margin="0,0,6,0"
                                    FontSize="10"
                                    Foreground="{DynamicResource TextDimBrush}"
                                    Text="作者" />
                                <TextBox
                                    Width="132"
                                    Padding="8,4"
                                    Text="{Binding PathHistoryAuthorFilter, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <StackPanel
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    Margin="0,0,6,0"
                                    FontSize="10"
                                    Foreground="{DynamicResource TextDimBrush}"
                                    Text="时间" />
                                <ListBox
                                    MinHeight="24"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    ItemsSource="{Binding PathHistoryRecentDaysOptions}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                    SelectedIndex="{Binding PathHistoryRecentDaysIndex, Mode=TwoWay}">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Margin" Value="0,0,6,0" />
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="Background" Value="Transparent" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                                            <Setter Property="BorderThickness" Value="0" />
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                            <Setter Property="Focusable" Value="False" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border
                                                            x:Name="Root"
                                                            MinWidth="46"
                                                            Padding="10,4"
                                                            Background="{DynamicResource Bg3Brush}"
                                                            BorderBrush="{DynamicResource BorderBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="6"
                                                            SnapsToDevicePixels="True">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface2Brush}" />
                                                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource HoverBrush}" />
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter Property="Opacity" Value="0.5" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                FontSize="10"
                                                FontWeight="SemiBold"
                                                Text="{Binding}" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="2" Margin="0,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.25*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="0.95*" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Grid.Column="0"
                                Padding="10,8"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0" Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            FontSize="10"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource TextDimBrush}"
                                            Text="提交历史" />
                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{DynamicResource TextDimBrush}"
                                            Text="{Binding PathHistoryRecentDaysLabel}" />
                                    </Grid>

                                    <ListView
                                        Grid.Row="1"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        ItemsSource="{Binding PathHistoryItems}"
                                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        SelectedItem="{Binding SelectedPathHistoryItem, Mode=TwoWay}"
                                        Visibility="{Binding HasPathHistoryItems, Converter={StaticResource BoolToVisibility}}">
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Margin" Value="0,0,0,4" />
                                                <Setter Property="Padding" Value="0" />
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="BorderThickness" Value="0" />
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                                <Setter Property="IsTabStop" Value="False" />
                                                <Style.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource MenuHoverBrush}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListView.ItemContainerStyle>
                                        <ListView.ItemTemplate>
                                            <DataTemplate DataType="{x:Type model:CommitHistoryItem}">
                                                <Border
                                                    Padding="10,7"
                                                    Background="{DynamicResource Bg2Brush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="6">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="82" />
                                                            <ColumnDefinition Width="116" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                            <TextBlock
                                                                FontFamily="{DynamicResource CodeFont}"
                                                                FontSize="11"
                                                                FontWeight="SemiBold"
                                                                Foreground="{DynamicResource AccentBrush}"
                                                                Text="{Binding ShortSha}" />
                                                            <TextBlock
                                                                Margin="0,2,0,0"
                                                                FontSize="9"
                                                                Foreground="{DynamicResource TextDimBrush}"
                                                                Text="{Binding CommitTimeText}" />
                                                        </StackPanel>
                                                        <TextBlock
                                                            Grid.Column="1"
                                                            VerticalAlignment="Center"
                                                            FontSize="10"
                                                            Foreground="{DynamicResource Accent2Brush}"
                                                            Text="{Binding Author}"
                                                            TextTrimming="CharacterEllipsis" />
                                                        <TextBlock
                                                            Grid.Column="2"
                                                            Margin="10,0,0,0"
                                                            VerticalAlignment="Center"
                                                            FontSize="11"
                                                            Foreground="{DynamicResource TextBrush}"
                                                            Text="{Binding MessageShort}"
                                                            TextTrimming="CharacterEllipsis" />
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>

                                    <Border
                                        Background="{DynamicResource BgBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Visibility="{Binding HasPathHistoryItems, Converter={StaticResource InverseBoolToVisibility}}">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock
                                                Margin="0,0,0,6"
                                                HorizontalAlignment="Center"
                                                FontSize="22"
                                                Text="🕘" />
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                FontSize="11"
                                                Foreground="{DynamicResource TextDimBrush}"
                                                Text="暂无提交记录" />
                                        </StackPanel>
                                    </Border>

                                    <Grid Background="{DynamicResource BgBrush}" Visibility="{Binding IsPathHistoryLoading, Converter={StaticResource BoolToVisibility}}">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock
                                                Margin="0,0,0,6"
                                                HorizontalAlignment="Center"
                                                FontSize="18"
                                                Text="⏳" />
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                FontSize="11"
                                                Foreground="{DynamicResource TextDimBrush}"
                                                Text="正在加载提交记录..." />
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>

                            <GridSplitter
                                Grid.Column="1"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch" />

                            <Border
                                Grid.Column="2"
                                Padding="10,8"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock
                                                FontSize="11"
                                                FontWeight="SemiBold"
                                                Foreground="{DynamicResource AccentBrush}"
                                                Text="{Binding SelectedCommitTitle}" />
                                            <TextBlock
                                                Margin="0,2,0,0"
                                                FontSize="10"
                                                Foreground="{DynamicResource TextDimBrush}"
                                                Text="{Binding SelectedCommitDetail.Commit.CommitTimeText}" />
                                        </StackPanel>
                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{DynamicResource TextDimBrush}"
                                            Text="{Binding SelectedCommitDetail.Commit.Sha}" />
                                    </Grid>

                                    <TextBlock
                                        Grid.Row="1"
                                        Margin="0,6,0,0"
                                        FontSize="11"
                                        Foreground="{DynamicResource TextBrush}"
                                        Text="{Binding SelectedCommitDetail.Commit.Message}"
                                        TextWrapping="Wrap" />

                                    <Grid Grid.Row="2" Margin="0,10,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TextBlock
                                            FontSize="10"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource TextDimBrush}"
                                            Text="影响文件" />
                                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding SelectedCommitDetail.Files}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type model:CommitFileChangeItem}">
                                                        <Grid Margin="0,4,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="22" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <Border
                                                                Width="18"
                                                                Height="18"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Background="{DynamicResource BgBrush}"
                                                                BorderBrush="{DynamicResource BorderBrush}"
                                                                BorderThickness="1"
                                                                CornerRadius="4">
                                                                <TextBlock
                                                                    HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"
                                                                    FontSize="10"
                                                                    FontWeight="SemiBold"
                                                                    Foreground="{DynamicResource Accent2Brush}"
                                                                    Text="{Binding StatusCode}" />
                                                            </Border>
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="8,0,0,0"
                                                                VerticalAlignment="Center"
                                                                FontSize="10"
                                                                Foreground="{DynamicResource TextDimBrush}"
                                                                Text="{Binding DisplayPath}"
                                                                TextTrimming="CharacterEllipsis" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>

                                    <Border
                                        Background="{DynamicResource BgBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Visibility="{Binding SelectedCommitDetail, Converter={StaticResource NullToVisibility}}">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock
                                                Margin="0,0,0,6"
                                                HorizontalAlignment="Center"
                                                FontSize="22"
                                                Text="⌁" />
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                FontSize="11"
                                                Foreground="{DynamicResource TextDimBrush}"
                                                Text="点击左侧提交查看详情" />
                                        </StackPanel>
                                    </Border>

                                    <Grid Background="{DynamicResource BgBrush}" Visibility="{Binding IsCommitDetailLoading, Converter={StaticResource BoolToVisibility}}">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock
                                                Margin="0,0,0,6"
                                                HorizontalAlignment="Center"
                                                FontSize="18"
                                                Text="⏳" />
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                FontSize="11"
                                                Foreground="{DynamicResource TextDimBrush}"
                                                Text="正在加载提交详情..." />
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </DockPanel>
</views:ThemedWindow>
